---
title: Database Layer
files:
  - src/db/**
  - src/models/**
acceptance:
  - Users and notes are persisted
  - A migration creates the schema
  - The data layer is isolated behind a repository interface
---

# Database Layer

Add a persistence layer for users and notes.

## Details

- Define `User` and `Note` models.
- Provide a migration that creates the tables/collections.
- Expose repository functions (`findUserByEmail`, `createNote`, ...) so routes never
  touch the database driver directly.
